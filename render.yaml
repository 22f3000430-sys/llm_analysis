services:
  - type: web
    name: llm-quiz
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      MY_SECRET: MYTDSLLM2025
      # OPENAI_API_KEY will be added in Render dashboard